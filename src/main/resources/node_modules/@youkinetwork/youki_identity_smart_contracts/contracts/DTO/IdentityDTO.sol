// SPDX-License-Identifier: MIT
pragma solidity >=0.8.0 <0.9.0;
import "../libraries/IdentityLib.sol";


    struct IdentityDTO {
        string  salutation;
        string  firstname;
        string  lastname;
        string  email;
        string  mobileNumber;
        address walletAddress;
        PersonType personType;
        uint[]  roles;
        string  unhashedEmail; // to take off we don't need we will rehahs the hashed email and save it in the list of emails 
    }

