# WattifySmartContracts

Blockchain stuff for Wattify dApp

## Package import

In order to import private npm packages, you will need to set the `NPM_TOKEN` environment variable as follows:

```console
> export NPM_TOKEN=<token>
```
